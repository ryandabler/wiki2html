{
  "name": "wiki2html",
  "version": "0.0.0",
  "description": "A Javascript parser that converts wikitext markup into HTML",
  "main": "parser.js",
  "scripts": {
    "test": "mocha --harmony --exit",
    "coverage": "node --harmony node_modules/istanbul/lib/cli.js cover node_modules/mocha/bin/_mocha -- -R spec ./test/*"
  },
  "author": "Ryan Dabler",
  "license": "GPL-3.0",
  "devDependencies": {
    "chai": "^4.1.2",
    "coveralls": "^3.0.0",
    "istanbul": "^0.4.5",
    "mocha": "^5.0.1"
  }
}
